version: '3.8'
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8010:80"
    # volumes:
    #   - .:/var/www/html
    environment:
      - MYSQL_HOST=172.21.0.2
      - MYSQL_PORT=3306
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
      - MYSQL_DATABASE=cms
    networks:
      - cms-network

networks:
  cms-network:
    driver: bridge
